export type Disponibilidade = {
    id: string;
    professor_id: string;
    dia_semana: number;
    hora_inicio: string;
    hora_fim: string;
}

export type DisponibilidadeEdicao = {
    professor_id: string;
    dia_semana: number;
    hora_inicio: string;
    hora_fim: string;
}